using UnityEngine;

namespace View.Factory
{
    public class ShipFactory : MonoBehaviour
    {
        // Create Model, Presenter, and View

        // Model depends on ShipData

        // Presenter depends on Model and View

        // View depends on Presenter
    }
}